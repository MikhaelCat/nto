### 1. **Создание признаков пользователя (`create_user_features`)**
Для каждого пользователя строятся **статистики на основе его взаимодействий в `train.csv`**:

- **Метаданные** из `users.csv`: возраст, пол.
- **Временные признаки**:
  - Дней с последнего взаимодействия
  - Частота активности (взаимодействий в день)
  - Предпочтение по времени суток (ночь/день) и дням недели
- **Поведенческие признаки**:
  - Доля прочитанных книг (`read_ratio`)
  - Общее количество взаимодействий
  - Скорость чтения (сколько книг читает в день)
  - Соотношение запланированных и прочитанных книг
- **Жанровые предпочтения** (если есть `book_genres.csv`):
  - Энтропия жанров (насколько разнообразны вкусы)
  - Топ-жанры
  - Жанровое разнообразие

`DataFrame` с индексом `user_id` и колонками — фичами.

---

### 2. **Создание признаков книги (`create_book_features`)**
Для каждой книги строятся фичи на основе:
- **Метаданных** из `books.csv`: год публикации, автор, язык, средний рейтинг.
- **Статистик взаимодействий** из `train.csv`:
  - Сколько раз книгу добавляли/читали
  - Доля прочитанных от всех взаимодействий
  - Средняя оценка от пользователей
  - Уникальное число читающих
- **Признаки автора**:
  - Популярность автора
  - Доля прочитанных его книг
- **Производные признаки**:
  - `publication_decade` (например, 2020)
  - `is_recent` (книга новее 2010 года?)

> Результат: `DataFrame` с индексом `book_id`.

---

### 3. **Текстовые признаки (`create_text_features`)**
Если есть `book_descriptions.csv`:

- **TF-IDF**: биграммы + снижение размерности через SVD → ~100 числовых фич.
- **BERT-эмбеддинги**:  
  - Используется предобученная `DeepPavlov/rubert-base-cased`
  - Генерируется вектор размером 768 для каждой книги
  - Кэшируется на диск, чтобы не пересчитывать каждый раз
  - Для книг без описания — заполняется нулями

> Это самые "дорогие" и информативные фичи.

---

### 4. **Формирование пар "пользователь–книга" (`create_two_towers_features`)**
Для каждого пользователя из `targets.csv` и его списка кандидатов из `candidates.csv`:

- Извлекаются **все фичи пользователя** (`user_{feature}`)
- Для **каждой книги-кандидата** — извлекаются **все фичи книги** (`book_{feature}`, `text_{feature}`)
- Формируется строка с совмещёнными признаками

> Результат: таблица, где **одна строка = одна книга для одного пользователя**

---

### 5. **Маркировка взаимодействий (`add_interaction_flags`)**
Поскольку из `train.csv` мы **знаем, какие книги пользователь уже видел**, добавляем:

- `has_interaction`: 1 — если пользователь уже взаимодействовал с книгой в обучающем периоде
- `interaction_has_read`: 1 — если прочитал, 0 — если только добавил в планы, -1 — если не взаимодействовал

❗**Важно**: `candidates.csv` включает:
- Книги, с которыми пользователь **взаимодействовал в тестовом периоде** (но мы этого не знаем при обучении!)
- **"Холодные" кандидаты** — книги, с которыми пользователь **никогда не взаимодействовал**

→ Поэтому `has_interaction = 0` **не означает**, что книга "холодная" — она может быть прочитанной в тестовом периоде!  
Но на этапе **обучения** мы можем использовать только историю из `train.csv`.

---

### 6. **Фильтрация "холодных" кандидатов (`filter_cold_candidates`)**
Это **вспомогательная функция** (в текущем виде — заглушка), но её смысл:

- "Холодные" кандидаты — это книги, которые:
  - Пользователь **никогда не видел** (нет в `train.csv`)
  - И не являются популярными / релевантными его профилю

→ На практике, **их не нужно фильтровать полностью**, а нужно **понизить их релевантность** (дать класс 0).

## 🎯 Как это используется дальше?

1. Полученная таблица с фичами (`user_...`, `book_...`, `text_...`) подаётся в **ранжирующую модель** (например, `LGBMRanker`).
2. Модель учится предсказывать **релевантность 0/1/2** на основе этих фичей.
3. На инференсе — для каждого пользователя ранжируем его `candidates` по предсказанной релевантности.
4. Отправляем топ-20 в `submission.csv`.
